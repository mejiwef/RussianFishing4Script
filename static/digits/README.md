# 数字模板目录

此目录存放拖钓巡航 OCR 使用的数字模板图片。

## 需要的文件

- `0.png` ~ `9.png` — 数字 0 到 9 的模板
- `colon.png` — 冒号 `:` 的模板

## 获取方式

运行校准工具从游戏截图中自动提取：

```cmd
.\venv\Scripts\python.exe -m rf4s.controller.calibrate
```

或手动获取：
1. 在游戏中截取右下角坐标显示区域
2. 裁切出每个数字字符，保存为单独的 PNG 文件
3. 确保背景为黑色，文字为白色（二值化后的效果）
